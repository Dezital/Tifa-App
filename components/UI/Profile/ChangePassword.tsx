import { View, Text } from 'react-native'
import React from 'react'

const ChangePassword = ({setMenuitem}: {setMenuitem: React.Dispatch<React.SetStateAction<string>>}) => {
  return (
    <View>
      <Text>ChangePassword</Text>
    </View>
  )
}

export default ChangePassword