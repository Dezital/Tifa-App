import { View, Text } from 'react-native'
import React from 'react'

const Notifications = ({setMenuitem}: {setMenuitem: React.Dispatch<React.SetStateAction<string>>}) => {
  return (
    <View>
      <Text>Notifications</Text>
    </View>
  )
}

export default Notifications