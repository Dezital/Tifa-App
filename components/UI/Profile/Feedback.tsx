import { View, Text } from 'react-native'
import React from 'react'

const Feedback = ({setMenuitem}: {setMenuitem: React.Dispatch<React.SetStateAction<string>>}) => {
  return (
    <View>
      <Text>Feedback</Text>
    </View>
  )
}

export default Feedback