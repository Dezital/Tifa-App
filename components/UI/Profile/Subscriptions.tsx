import { View, Text } from 'react-native'
import React from 'react'

const Subscriptions = ({setMenuitem}: {setMenuitem: React.Dispatch<React.SetStateAction<string>>}) => {
  return (
    <View>
      <Text>Subscriptions</Text>
    </View>
  )
}

export default Subscriptions